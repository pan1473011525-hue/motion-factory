# Product

## Register

product

## Users

主要用户是一名在 macOS 上工作的摄像剪辑师，通常在低光后期环境中操作。核心任务是快速选择动效模板，或从组件与动效库自由编排画面，预览透明叠加效果，并导出能直接进入剪辑时间线的透明通道视频。

## Product Purpose

Motioner 是一款个人使用的动效生成工具。它通过受约束的模板、可组合的图层与清晰的参数检查器，替代重复的关键帧、排版和透明导出工作。成功意味着常见动效能在几分钟内完成，预览与导出一致，输出的帧率、时长、分辨率和 alpha 均可验证。

## Brand Personality

精确、克制、可靠。界面应让熟悉剪辑、调色和图形工具的用户立即信任，不用重新学习常见桌面软件的操作语法。

## Anti-references

- 不像营销型 SaaS 仪表盘，不使用大量同质圆角卡片。
- 不使用 AI 紫色渐变、装饰性玻璃拟态和无意义发光。
- 不模仿完整非线性剪辑软件；图层时间轴只服务于动效编排，不引入镜头剪辑、音频混合或任意关键帧曲线复杂度。
- 不用装饰性动画打断编辑流程，不隐藏错误和导出状态。
- 不用过度定制的表单、滚动条或陌生交互替代标准 macOS/桌面工具习惯。

## Design Principles

1. 画布优先：中间预览是工作台的视觉中心，参数与模板服务于画面。
2. 快速到结果：常用参数直接可见，高级选项渐进展开，不制造配置负担。
3. 约束带来自信：模板明确数据上限、时长模式和输出能力，错误在导出前出现。
4. 状态必须可见：保存、缺失素材、渲染阶段、进度、失败原因和输出核验都明确反馈。
5. 同源与确定性：预览和导出复用同一组件，动画由帧号驱动，相同输入得到相同输出。

## Accessibility & Inclusion

- 正文和控件达到 WCAG AA 对比度。
- 所有表单和主操作支持键盘导航并提供清晰焦点状态。
- 状态不只依赖颜色表达，错误、警告和成功均有文字说明。
- 界面过渡尊重 `prefers-reduced-motion`；模板提供全局减少动效选项。
- 中文、英文、数字和长文本均需有明确的溢出策略。
